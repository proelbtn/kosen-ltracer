#include "motor.h"

void motor_set_mode_left(unsigned char mode) {
    switch (mode) {
        case:
            break;
        case:
            break;
        case:
            break;
        case:
            break;
    }
}

void motor_set_mode_right(unsigned char mode) {
    switch (mode) {
        case:
            break;
        case:
            break;
        case:
            break;
        case:
            break;
    }
}